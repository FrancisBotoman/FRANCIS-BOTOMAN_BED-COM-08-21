#include <iosream>
#include <string>
using namespace std;
int main()
{
string elements[] = {"B123", "C234", "A345", "C15", "B177", "G3003", "C235", "B179"}; //array declaration and initialisation

for(const auto& element: elements){
    //check if elements start with letter 'B'
    if(element[0] == 'B' {
        cout << element << endl; //output element starting with 'B'
    })
}
    return 0;
}